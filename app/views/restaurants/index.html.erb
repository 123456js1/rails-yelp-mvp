<h1>Restaurants in Lisbon</h1>
<ul class="list-group">
  <% @restaurants.each do |restaurant| %>
    <li class="list-group-item"><%= link_to restaurant.name, restaurant %></li>
  <% end %>
</ul>

<p><%= link_to "+ Add Restaurant", new_restaurant_path(@restaurant) %></p>
